{
  "name": "deka-sir-classes-app",
  "version": "1.0.0",
  "private": true,
  "description": "Native Android/iOS wrapper for DEKA SIR CLASSES",
  "scripts": {
    "sync": "npx cap sync",
    "android": "npx cap open android",
    "ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
